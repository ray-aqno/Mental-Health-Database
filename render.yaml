services:
  - type: web
    name: mental-health-web
    env: dotnet
    plan: free
    buildCommand: 'dotnet publish -c Release -o publish'
    startCommand: 'dotnet publish/MentalHealthDatabase.dll'
    envVars:
      - key: DATABASE_URL
        fromDatabase: false
