[pytest]
# Scraper source modules live in Scripts/; tests live in Scripts/tests/.
# Add Scripts/ to the import path so tests can `from simple_scraper import ...`
# without the source and tests sharing a directory.
pythonpath = Scripts
testpaths = Scripts/tests
